# Telegram Bot для водителей

Отдельный проект Telegram бота для управления водителями. Работает совместно с админ-панелью через общую базу данных Supabase.

## 🚀 Быстрый старт

1. **Установка:**
   ```bash
   npm install
   ```

2. **Настройка:**
   ```bash
   cp .env.example .env
   # Отредактируйте .env и заполните все переменные
   ```

3. **Запуск:**
   ```bash
   npm start
   ```

## 📋 Переменные окружения

Создайте файл `.env` на основе `.env.example`:

```env
BOT_TOKEN=your_telegram_bot_token
ADMIN_CHAT_ID=your_admin_chat_id
SUPABASE_URL=https://fbyjqfsqpwyjkzhscpkg.supabase.co
SUPABASE_ANON_KEY=your_supabase_anon_key
TZ=Europe/Vilnius
```

**Важно:** `SUPABASE_URL` и `SUPABASE_ANON_KEY` должны быть **такими же**, как в админке!

## 🎯 Функции

- ✅ Обработка команды `/start` с персональным токеном
- ✅ Прием локаций от водителей
- ✅ Ежедневные напоминания в 9:00 (cron)
- ✅ Проверка дат начала и окончания напоминаний
- ✅ Автоматическое завершение маршрута после end_date
- ✅ Отправка уведомлений админу о новых локациях

## 📦 Деплой

### Railway (рекомендуется)

Следуйте инструкциям в [DEPLOY.md](./DEPLOY.md)

**Кратко:**
1. Зайдите на [railway.app](https://railway.app)
2. Создайте проект из GitHub репозитория
3. Добавьте переменные окружения
4. Railway автоматически задеплоит

### Другие варианты

- Render
- Fly.io
- VPS (DigitalOcean, Hetzner, AWS EC2)

Подробности в [DEPLOY.md](./DEPLOY.md)

## 🔗 Связь с админкой

Бот и админка используют **одну и ту же базу данных Supabase**:
- Водители, созданные в админке, сразу видны боту
- Локации, отправленные ботом, сразу видны в админке
- Даты напоминаний, установленные в админке, работают в боте

## 📚 Документация

- [DEPLOY.md](./DEPLOY.md) - Подробная инструкция по деплою
- [NEXT_STEPS.md](./NEXT_STEPS.md) - Что делать дальше после клонирования
- [QUICK_START.md](./QUICK_START.md) - Быстрый старт для локальной разработки

## ✅ Проверка работы

После деплоя отправьте боту:
- `/whoami` - вернет ваш chat.id
- `/testadmin` - отправит тестовое сообщение в админ-чат
- Перейдите по ссылке с токеном водителя - должно прийти приветственное сообщение

## 🛠️ Разработка

```bash
# Установка зависимостей
npm install

# Запуск в режиме разработки
npm run dev

# Запуск в продакшене
npm start
```

## 📝 Структура проекта

```
mvp-tg-bot/
├── bot.js              # Основной файл бота
├── lib/
│   ├── db.js          # Функции работы с БД
│   └── supabase.js    # Клиент Supabase
├── package.json
├── .env.example       # Пример переменных окружения
└── README.md
```

## 🔧 Технологии

- **Node.js** - среда выполнения
- **Telegraf** - библиотека для Telegram Bot API
- **Supabase** - база данных (PostgreSQL)
- **node-cron** - планировщик задач для ежедневных напоминаний

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в Railway/другой платформе
2. Убедитесь, что все переменные окружения установлены
3. Проверьте, что база данных Supabase доступна
4. Убедитесь, что миграция с полями `reminder_start_date` и `reminder_end_date` применена
