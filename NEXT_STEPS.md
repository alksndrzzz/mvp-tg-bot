# Что делать дальше

## ✅ Текущее состояние

Проект `mvp-tg-bot` готов к деплою. Код актуален и включает:
- ✅ Обработку команды `/start` с токеном
- ✅ Прием локаций от водителей
- ✅ Ежедневные напоминания в 9:00 (cron)
- ✅ Проверку дат начала и окончания напоминаний
- ✅ Автоматическое завершение маршрута после end_date

## 📋 Чек-лист перед деплоем

### 1. Проверка кода

- [x] Код бота актуален
- [x] Все зависимости в `package.json`
- [x] `.gitignore` настроен
- [x] Документация обновлена

### 2. Подготовка репозитория

```bash
cd mvp-tg-bot

# Проверьте, что все файлы на месте
ls -la

# Инициализируйте git (если еще не сделано)
git init

# Добавьте все файлы
git add .

# Создайте первый коммит
git commit -m "Initial commit: Telegram bot for drivers"

# Создайте репозиторий на GitHub и запушьте
# git remote add origin https://github.com/your-username/mvp-tg-bot.git
# git push -u origin main
```

### 3. Получение токена бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и получите токен
4. Сохраните токен - он понадобится для деплоя

### 4. Получение chat.id админа

После деплоя бота:
1. Отправьте боту команду `/whoami`
2. Бот вернет ваш chat.id
3. Используйте этот ID для переменной `ADMIN_CHAT_ID`

## 🚀 Деплой на Railway

Следуйте инструкциям в [DEPLOY.md](./DEPLOY.md)

**Краткая версия:**
1. Зайдите на [railway.app](https://railway.app)
2. Создайте проект из GitHub репозитория
3. Добавьте переменные окружения:
   - `BOT_TOKEN` - от BotFather
   - `ADMIN_CHAT_ID` - получите после первого запуска через `/whoami`
   - `SUPABASE_URL` - из Supabase Dashboard
   - `SUPABASE_ANON_KEY` - из Supabase Dashboard (те же, что в админке!)
   - `TZ=Europe/Vilnius` - ваш часовой пояс
4. Railway автоматически задеплоит

## 🔗 Связь с админкой

**Важно:** Бот и админка используют **одну и ту же базу данных Supabase**.

Это означает:
- Водители, созданные в админке, сразу видны боту
- Локации, отправленные ботом, сразу видны в админке
- Даты напоминаний, установленные в админке, работают в боте

**Переменные Supabase должны быть одинаковыми в обоих проектах!**

## 📝 Проверка после деплоя

1. **Проверка бота:**
   ```bash
   # Отправьте боту в Telegram:
   /whoami        # Должен вернуть ваш chat.id
   /testadmin     # Должно прийти сообщение в админ-чат
   ```

2. **Проверка связи с админкой:**
   - Создайте водителя в админке
   - Скопируйте ссылку на бота
   - Перейдите по ссылке - должно прийти приветственное сообщение

3. **Проверка дат напоминаний:**
   - Установите дату окончания в админке (например, вчерашнюю дату)
   - Отправьте локацию боту - должно прийти сообщение о завершении маршрута

## 🐛 Troubleshooting

### Бот не отвечает

- Проверьте логи в Railway
- Убедитесь, что `BOT_TOKEN` правильный
- Проверьте, что бот запущен (должно быть "Bot started (long polling)…")

### Ошибки подключения к Supabase

- Проверьте `SUPABASE_URL` и `SUPABASE_ANON_KEY`
- Убедитесь, что они совпадают с теми, что в админке
- Проверьте, что миграция с полями `reminder_start_date` и `reminder_end_date` применена

### Cron задачи не работают

- Проверьте переменную `TZ` (должна быть правильная таймзона)
- Проверьте логи в Railway в момент выполнения (9:00 по TZ)
- Убедитесь, что бот работает 24/7 (Railway не должен останавливать процесс)

## 📚 Дополнительная документация

- [DEPLOY.md](./DEPLOY.md) - Подробная инструкция по деплою
- [README.md](./README.md) - Общая информация о проекте
- [QUICK_START.md](./QUICK_START.md) - Быстрый старт для локальной разработки

## ✅ Готово!

После успешного деплоя:
- Бот будет работать 24/7 на Railway
- Ежедневные напоминания будут отправляться автоматически
- Водители смогут отправлять локации
- Админка будет видеть все данные в реальном времени

