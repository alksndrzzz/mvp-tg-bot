# Функция "Новый маршрут" для бота

## Описание

Когда маршрут водителя завершен (статус `'stopped'` и дата окончания `journey_end_date` прошла), админ-панель блокирует редактирование дат и показывает кнопку "🆕 НОВЫЙ МАРШРУТ". При нажатии на эту кнопку создается новый маршрут для водителя.

## Логика работы

### Определение завершенного маршрута

Маршрут считается **завершенным**, если выполняются оба условия:
1. `route_status = 'stopped'`
2. `journey_end_date < CURRENT_DATE` (дата окончания прошла)

### Создание нового маршрута

Когда админ нажимает кнопку "🆕 НОВЫЙ МАРШРУТ" в админ-панели:

1. **API запрос:** `PUT /api/drivers/[id]` с телом:
   ```json
   {
     "createNewRoute": true,
     "journeyStartDate": "2024-12-15",
     "journeyEndDate": "2024-12-25"
   }
   ```

2. **Обновление в БД:**
   - `route_status` устанавливается в `'not-started-yet'`
   - `is_active` устанавливается в `false`
   - `journey_start_date` устанавливается в новую дату начала
   - `journey_end_date` устанавливается в новую дату окончания

3. **Уведомление водителя:**
   - Бот должен отправить уведомление водителю при следующем взаимодействии с ботом
   - Уведомление должно сообщать о новом маршруте

## Что нужно реализовать в боте

### 1. Определение нового маршрута

Бот должен проверять, был ли создан новый маршрут для водителя. Это можно определить по:

- `route_status = 'not-started-yet'` 
- И `journey_start_date` и `journey_end_date` установлены (не NULL)
- И водитель уже был активирован ранее (есть `telegram_chat_id`)

**Важно:** При первом создании водителя в админке также будет `route_status = 'not-started-yet'`, но водитель еще не активирован (нет `telegram_chat_id`). В этом случае просто отправляем приветственное сообщение.

### 2. Уведомление о новом маршруте

Когда водитель взаимодействует с ботом (отправляет `/start` или локацию), бот должен:

1. Проверить статус водителя
2. Если `route_status = 'not-started-yet'` и есть `telegram_chat_id` (водитель уже был активирован), значит это новый маршрут
3. Отправить уведомление:

```javascript
// Пример уведомления
const message = `🚗 У вас новый маршрут!

📅 Дата начала: ${formatDate(driver.journey_start_date)}
📅 Дата окончания: ${formatDate(driver.journey_end_date)}

Нажмите "📍 Отправить локацию" чтобы начать поездку.`;
```

### 3. Пример реализации

```javascript
// При обработке команды /start или отправке локации
async function handleDriverInteraction(ctx) {
  const chatId = ctx.from.id;
  
  // Найти водителя по telegram_chat_id
  const driver = await getDriverByChatId(chatId);
  
  if (!driver) {
    // Водитель не найден - это новый водитель, обработать /start с токеном
    return handleNewDriver(ctx);
  }
  
  // Проверить, создан ли новый маршрут
  if (driver.route_status === 'not-started-yet' && 
      driver.journey_start_date && 
      driver.journey_end_date) {
    
    // Это новый маршрут - отправить уведомление
    await ctx.reply(
      `🚗 У вас новый маршрут!\n\n` +
      `📅 Дата начала: ${formatDate(driver.journey_start_date)}\n` +
      `📅 Дата окончания: ${formatDate(driver.journey_end_date)}\n\n` +
      `Нажмите "📍 Отправить локацию" чтобы начать поездку.`,
      {
        reply_markup: {
          keyboard: [
            [{ text: '📍 Отправить локацию', request_location: true }]
          ],
          resize_keyboard: true,
        }
      }
    );
  }
  
  // Продолжить обычную обработку...
}
```

### 4. SQL запрос для проверки

```sql
-- Получить водителя с новым маршрутом
SELECT * 
FROM drivers 
WHERE telegram_chat_id = :chatId
  AND route_status = 'not-started-yet'
  AND journey_start_date IS NOT NULL
  AND journey_end_date IS NOT NULL;
```

## Важные моменты

1. **Не путать с первым созданием водителя:**
   - При первом создании водителя в админке: `telegram_chat_id = NULL`
   - При создании нового маршрута: `telegram_chat_id` уже установлен

2. **Статус маршрута:**
   - После создания нового маршрута статус всегда `'not-started-yet'`
   - При первой отправке локации статус меняется на `'in-progress'`

3. **Уведомление отправляется один раз:**
   - После отправки уведомления водитель начинает отправлять локации
   - При первой локации статус меняется на `'in-progress'`
   - Больше уведомление о новом маршруте не отправляется

4. **Формат дат:**
   - Даты хранятся в формате `DATE` (YYYY-MM-DD)
   - При отображении водителю используйте формат, понятный для вашего региона

## Тестирование

Для тестирования функции:

1. Создайте водителя в админке
2. Активируйте водителя через бота (отправьте `/start` с токеном)
3. Завершите маршрут (нажмите "✅ Маршрут завершён" в боте)
4. Дождитесь, пока дата окончания пройдет
5. В админке нажмите "🆕 НОВЫЙ МАРШРУТ" и укажите новые даты
6. Взаимодействуйте с ботом от имени водителя
7. Должно прийти уведомление о новом маршруте

## API Reference

### Создание нового маршрута

**Endpoint:** `PUT /api/drivers/[id]`

**Request Body:**
```json
{
  "createNewRoute": true,
  "journeyStartDate": "2024-12-15",
  "journeyEndDate": "2024-12-25"
}
```

**Response:**
```json
{
  "id": "uuid",
  "name": "Иван Иванов",
  "route_status": "not-started-yet",
  "is_active": false,
  "journey_start_date": "2024-12-15",
  "journey_end_date": "2024-12-25",
  ...
}
```

**Что происходит в БД:**
- `route_status` → `'not-started-yet'`
- `is_active` → `false`
- `journey_start_date` → новая дата начала
- `journey_end_date` → новая дата окончания
- `telegram_chat_id` → остается без изменений (если был установлен)

