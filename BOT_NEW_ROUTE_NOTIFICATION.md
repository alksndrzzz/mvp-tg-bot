# Уведомления о новом маршруте в Telegram боте

## 📋 Обзор

Когда администратор создает новый маршрут для водителя через админ-панель, бот автоматически отправляет уведомление водителю в Telegram при его следующем взаимодействии с ботом.

---

## 🔄 Процесс создания нового маршрута

### В админ-панели

При создании нового маршрута через админ-панель происходит следующее:

1. **Вызывается функция** `createNewRoute(driverId, journeyStartDate, journeyEndDate)`
2. **Обновляется запись водителя в БД:**
   ```sql
   UPDATE drivers SET
     route_status = 'not-started-yet',
     is_active = false,
     journey_start_date = <новая дата начала>,
     journey_end_date = <новая дата окончания>
   WHERE id = <driver_id>;
   ```

### Определение нового маршрута

Новый маршрут определяется по следующим условиям:

- ✅ `route_status === 'not-started-yet'`
- ✅ `journey_start_date` установлена (не NULL)
- ✅ `journey_end_date` установлена (не NULL)
- ✅ `telegram_chat_id` установлен (водитель уже был активирован ранее)

> **Важно:** При первом создании водителя в админке также будет `route_status = 'not-started-yet'`, но водитель еще не активирован (нет `telegram_chat_id`). В этом случае просто отправляется приветственное сообщение.

---

## 🚀 Реализация в боте

### 1. Функция проверки нового маршрута

Функция `isNewRoute()` уже реализована в `lib/db.js`:

```javascript
function isNewRoute(driver) {
  if (!driver) return false;
  
  const hasRouteStatus = driver.route_status === 'not-started-yet';
  const hasDates = driver.journey_start_date && driver.journey_end_date;
  const wasActivated = driver.telegram_chat_id !== null && driver.telegram_chat_id !== undefined;
  
  return hasRouteStatus && hasDates && wasActivated;
}
```

### 2. Функция форматирования даты

Функция `formatDateForDriver()` уже реализована в `lib/db.js`:

```javascript
function formatDateForDriver(dateStr) {
  if (!dateStr) return '';
  
  try {
    const date = new Date(dateStr + 'T00:00:00');
    const day = String(date.getDate()).padStart(2, '0');
    const month = String(date.getMonth() + 1).padStart(2, '0');
    const year = date.getFullYear();
    return `${day}.${month}.${year}`;
  } catch (error) {
    console.error('Error formatting date:', error);
    return dateStr;
  }
}
```

### 3. Где проверяется новый маршрут

Проверка нового маршрута уже реализована в следующих местах:

#### ✅ При обработке команды `/start`

```javascript
// bot.js, строки 188-200
if (db.isNewRoute(driver)) {
  console.log('[START] Обнаружен новый маршрут для водителя:', driver.id);
  const startDate = db.formatDateForDriver(driver.journey_start_date);
  const endDate = db.formatDateForDriver(driver.journey_end_date);
  
  await ctx.reply(
    `🚗 У вас новый маршрут!\n\n` +
    `📅 Дата начала: ${startDate}\n` +
    `📅 Дата окончания: ${endDate}\n\n` +
    `Нажмите "📍 Отправить местоположение" чтобы начать поездку.`,
    keyboard
  );
}
```

#### ✅ При получении локации

```javascript
// bot.js, строки 313-334
if (db.isNewRoute(driver)) {
  console.log('[LOCATION] Обнаружен новый маршрут для водителя:', driver.id);
  const startDate = db.formatDateForDriver(driver.journey_start_date);
  const endDate = db.formatDateForDriver(driver.journey_end_date);
  
  await ctx.reply(
    `🚗 У вас новый маршрут!\n\n` +
    `📅 Дата начала: ${startDate}\n` +
    `📅 Дата окончания: ${endDate}\n\n` +
    `Нажмите "📍 Отправить местоположение" чтобы начать поездку.`,
    keyboard
  );
  // Продолжаем обработку локации после уведомления
}
```

---

## 📨 Формат уведомления

### Сообщение водителю

```
🚗 У вас новый маршрут!

📅 Дата начала: 15.12.2024
📅 Дата окончания: 25.12.2024

Нажмите "📍 Отправить местоположение" чтобы начать поездку.
```

### Клавиатура

После отправки уведомления водителю показывается клавиатура с кнопками:
- 📍 Отправить местоположение
- ✅ Маршрут завершён

---

## 🔍 Логика работы

### Сценарий 1: Водитель уже был активирован

1. Админ создает новый маршрут в админ-панели
2. В БД обновляется:
   - `route_status` → `'not-started-yet'`
   - `journey_start_date` → новая дата
   - `journey_end_date` → новая дата
3. Водитель взаимодействует с ботом (отправляет `/start` или локацию)
4. Бот проверяет `isNewRoute(driver)` → `true`
5. Бот отправляет уведомление о новом маршруте
6. Водитель видит даты и может начать отправлять локации

### Сценарий 2: Первое создание водителя

1. Админ создает водителя в админ-панели
2. В БД создается запись:
   - `route_status` → `'not-started-yet'`
   - `telegram_chat_id` → `NULL`
3. Водитель отправляет `/start <token>`
4. Бот проверяет `isNewRoute(driver)` → `false` (нет `telegram_chat_id`)
5. Бот отправляет обычное приветственное сообщение для новой поездки

---

## 🎯 Особенности реализации

### ✅ Что уже работает

- ✅ Проверка нового маршрута при `/start`
- ✅ Проверка нового маршрута при отправке локации
- ✅ Форматирование дат в формате ДД.ММ.ГГГГ
- ✅ Отправка уведомления с клавиатурой
- ✅ Обработка случая, когда маршрут остановлен, но создан новый

### 🔄 Обработка остановленного маршрута

Если маршрут был остановлен (`route_status = 'stopped'`), но админ создал новый маршрут:

```javascript
// bot.js, строки 152-165
if (routeStatus === 'stopped') {
  if (driver.journey_start_date && driver.journey_end_date && driver.telegram_chat_id) {
    // Новый маршрут создан админом - обновляем статус
    await db.setDriverRouteStatus(driver.id, 'not-started-yet');
    driver.route_status = 'not-started-yet';
    // Продолжаем обработку для отправки уведомления о новом маршруте
  } else {
    // Маршрут действительно остановлен, нового маршрута нет
    return ctx.reply('Спасибо, маршрут окончен. Если что - свяжитесь с администратором лично напрямую.', removeKeyboard);
  }
}
```

---

## 🔄 Альтернативный подход: Realtime подписка

> **Примечание:** Текущая реализация использует проверку при взаимодействии. Realtime подписка может быть добавлена в будущем для мгновенных уведомлений.

### Преимущества Realtime подписки

- ⚡ Уведомление отправляется **сразу** при создании нового маршрута
- 🎯 Не нужно ждать взаимодействия водителя с ботом
- 📡 Более отзывчивый пользовательский опыт

### Недостатки Realtime подписки

- 🔌 Требует постоянного подключения к Supabase Realtime
- ⚙️ Сложнее в настройке и отладке
- 💰 Может требовать дополнительных ресурсов

### Пример реализации (для будущего)

```javascript
// Подписка на изменения водителей
const channel = supabase
  .channel('drivers-updates')
  .on(
    'postgres_changes',
    {
      event: 'UPDATE',
      schema: 'public',
      table: 'drivers',
      filter: 'route_status=eq.not-started-yet',
    },
    async (payload) => {
      const driver = payload.new;
      
      // Проверяем, что это новый маршрут
      if (db.isNewRoute(driver) && driver.telegram_chat_id) {
        await BOT.telegram.sendMessage(
          driver.telegram_chat_id,
          `🚗 У вас новый маршрут!\n\n` +
          `📅 Дата начала: ${db.formatDateForDriver(driver.journey_start_date)}\n` +
          `📅 Дата окончания: ${db.formatDateForDriver(driver.journey_end_date)}\n\n` +
          `Нажмите "📍 Отправить местоположение" чтобы начать поездку.`,
          keyboard
        );
      }
    }
  )
  .subscribe();
```

---

## 📊 Диаграмма потока

```
Админ создает новый маршрут
         │
         ▼
Обновление БД:
- route_status = 'not-started-yet'
- journey_start_date = <дата>
- journey_end_date = <дата>
         │
         ▼
Водитель взаимодействует с ботом
    (команда /start или локация)
         │
         ▼
Бот проверяет isNewRoute(driver)
         │
    ┌────┴────┐
    │         │
   true     false
    │         │
    ▼         ▼
Отправка    Обычная
уведомления обработка
о новом
маршруте
```

---

## 🛠️ Технические детали

### Используемые функции из `lib/db.js`

- `isNewRoute(driver)` - проверка нового маршрута
- `formatDateForDriver(dateStr)` - форматирование даты
- `setDriverRouteStatus(driverId, 'not-started-yet')` - обновление статуса

### Используемые таблицы БД

- `drivers` - основная таблица водителей
  - `route_status` - статус маршрута
  - `journey_start_date` - дата начала поездки
  - `journey_end_date` - дата окончания поездки
  - `telegram_chat_id` - ID чата в Telegram

### Обработка ошибок

Все операции обернуты в `try-catch` блоки:

```javascript
try {
  await ctx.reply(/* уведомление */);
} catch (replyError) {
  if (replyError.response?.error_code === 403) {
    // Пользователь заблокировал бота
    return;
  }
  throw replyError;
}
```

---

## ✅ Чеклист реализации

- [x] Функция `isNewRoute()` реализована
- [x] Функция `formatDateForDriver()` реализована
- [x] Проверка при команде `/start` реализована
- [x] Проверка при отправке локации реализована
- [x] Обработка остановленного маршрута реализована
- [x] Форматирование дат в формате ДД.ММ.ГГГГ
- [x] Отправка уведомления с клавиатурой
- [ ] Realtime подписка (опционально, для будущего)

---

## 📝 Примеры использования

### Пример 1: Водитель получает новый маршрут

1. Админ создает новый маршрут с датами:
   - Начало: 2024-12-15
   - Окончание: 2024-12-25

2. Водитель отправляет `/start` или локацию

3. Водитель получает сообщение:
   ```
   🚗 У вас новый маршрут!
   
   📅 Дата начала: 15.12.2024
   📅 Дата окончания: 25.12.2024
   
   Нажмите "📍 Отправить местоположение" чтобы начать поездку.
   ```

### Пример 2: Маршрут был остановлен, создан новый

1. Водитель нажал "✅ Маршрут завершён"
2. Админ создал новый маршрут
3. Водитель отправляет `/start` или локацию
4. Бот автоматически обновляет статус на `'not-started-yet'`
5. Водитель получает уведомление о новом маршруте

---

## 🔗 Связанные документы

- [NEW_ROUTE_FEATURE.md](./NEW_ROUTE_FEATURE.md) - описание функции "Новый маршрут"
- [ADMIN_INTEGRATION.md](./ADMIN_INTEGRATION.md) - интеграция с админ-панелью
- [BOT.md](./BOT.md) - полная документация бота

---

## 📞 Поддержка

При возникновении проблем проверьте:

1. ✅ Водитель имеет `telegram_chat_id` (был активирован ранее)
2. ✅ `route_status = 'not-started-yet'`
3. ✅ `journey_start_date` и `journey_end_date` установлены
4. ✅ Водитель не заблокировал бота
5. ✅ Логи бота на наличие ошибок

---

**Последнее обновление:** 2024-12-19
